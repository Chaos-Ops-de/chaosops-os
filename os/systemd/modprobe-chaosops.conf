# Installed to /etc/modprobe.d/chaosops.conf
# No USB mass storage on the kiosk: nothing to mount, nothing to exfiltrate to.
# (USB HID — keyboards for the operator chords — is unaffected.)
blacklist usb_storage
blacklist uas
install usb_storage /bin/false
install uas /bin/false
